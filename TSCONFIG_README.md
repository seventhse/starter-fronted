# About `TSconfig.json`

> All config and description, generated by ChatGPT.
> [`TSconfig` Reference](https://www.typescriptlang.org/tsconfig/)

```json
{
  "compilerOptions": {
    "allowJs": true, // Allows JavaScript files to be compiled 允许编译 JavaScript 文件
    "allowSyntheticDefaultImports": true, // Allows default imports from modules with no default export 允许从没有默认导出的模块中默认导入
    "allowUmdGlobalAccess": true, // Allows accessing UMD globals from modules 允许从模块中访问 UMD 全局变量
    "alwaysStrict": true, // Parses in strict mode and emits "use strict" for each source file 解析为严格模式，并为每个源文件发出 "use strict"
    "baseUrl": ".", // Base directory to resolve non-relative module names 基目录以解析非相对模块名称
    "checkJs": false, // Enables type checking on JavaScript files 启用 JavaScript 文件的类型检查
    "declaration": true, // Generates corresponding .d.ts files 生成相应的 .d.ts 文件
    "declarationMap": true, // Generates source maps for d.ts files 生成 d.ts 文件的源映射
    "downlevelIteration": true, // Enables iterables to work with for..of, spread, and destructuring when targeting ES5 or ES3 在目标为 ES5 或 ES3 时，启用 iterables 工作以支持 for..of、spread 和 destructuring
    "emitDeclarationOnly": true, // Only emits d.ts files 仅生成 d.ts 文件
    "esModuleInterop": true, // Enables emit interoperability between CommonJS and ES Modules 启用 CommonJS 和 ES 模块之间的发射互操作性
    "exactOptionalPropertyTypes": true, // Interprets optional property types as written. 解释为编写的可选属性类型
    "experimentalDecorators": true, // Enables experimental support for decorators 启用装饰器的实验性支持
    "forceConsistentCasingInFileNames": true, // Ensures that casing is correct in imports 确保导入中的大小写正确
    "importHelpers": true, // Import emit helpers from tslib 导入 emit 帮助器从 tslib
    "incremental": true, // Enable incremental compilation 启用增量编译
    "inlineSourceMap": true, // Generates inline source maps 生成内联源映射
    "inlineSources": true, // Include source code in source maps 包括源代码在源映射中
    "isolatedModules": true, // Transpiles each file as a separate module 将每个文件编译为单独的模块
    "jsx": "react", // Specifies the JSX code generation 指定 JSX 代码生成方式
    "lib": ["ES6", "DOM"], // Specifies library files to be included 指定要包含的库文件
    "module": "CommonJS", // Specifies the module system 指定模块系统
    "moduleResolution": "node", // Specifies module resolution strategy 指定模块解析策略
    "newLine": "lf", // Specifies the end of line sequence to be used in output files 指定输出文件中使用的行结尾序列
    "noEmit": false, // Does not emit output 不生成输出
    "noEmitOnError": true, // Does not emit outputs if any type checking errors are reported 如果报告任何类型检查错误，则不生成输出
    "noFallthroughCasesInSwitch": true, // Reports errors for fallthrough cases in switch statements 报告 switch 语句中穿透案例的错误
    "noImplicitAny": true, // Raises an error on expressions and declarations with an implied any type 在具有隐含 any 类型的表达式和声明上发错误
    "noImplicitReturns": true, // Reports an error when not all code paths in function return a value 当函数中的所有代码路径不返回值时报告错误
    "noImplicitThis": true, // Raises error on this expressions with an implied any type 在具有隐含 any 类型的 this 表达式上引发错误
    "noUnusedLocals": true, // Reports errors on unused locals 报告未使用的本地变量错误
    "noUnusedParameters": true, // Reports errors on unused parameters 报告未使用的参数错误
    "preserveConstEnums": true, // Preserves the original const enum declarations 保留原始的 const 枚举声明
    "removeComments": true, // Do not emit comments 不生成注释
    "resolveJsonModule": true, // Allows importing modules with a .json extension 允许导入具有 .json 扩展名的模块
    "skipLibCheck": true, // Skips type checking of declaration files 跳过声明文件的类型检查
    "sourceMap": true, // Generates source map files 生成源映射文件
    "strict": true, // Enables all strict type-checking options 启用所有严格的类型检查选项
    "strictBindCallApply": true, // Checks that the arguments for bind, call, and apply methods match the original function 确保 bind、call 和 apply 方法的参数与原始函数匹配
    "strictFunctionTypes": true, // Ensures strict checking of function types 确保严格检查函数类型
    "strictNullChecks": true, // Ensures strict null checks 确保严格的空值检查
    "strictPropertyInitialization": true, // Ensures property initialization in classes 确保类中的属性初始化
    "target": "ES6", // Sets the JavaScript language version 设置生成的 JavaScript 语言版本
    "useUnknownInCatchVariables": true // Default catch clause variables as unknown instead of any 将默认 catch 子句变量设为 unknown 而不是 any
  },
  "include": ["src/**/*"], // Includes all TypeScript files in the src directory 包含 src 目录中的所有 TypeScript 文件
  "exclude": ["node_modules", "**/*.spec.ts"], // Excludes node_modules directory and test files 排除 node_modules 目录和测试文件
  "files": [], // List of individual files to be included 包含的单个文件列表
  "references": [ // List of project references 项目引用列表
    {
      "path": "./anotherProject" // Path to another project 另一个项目的路径
    }
  ]
}
```
